# Example: Enforcing Resource Quotas
# This ensures one team's service cannot crash the entire cluster.
apiVersion: v1
kind: ResourceQuota
metadata:
  name: team-alpha-quota
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 8Gi
    limits.cpu: "10"
    limits.memory: 16Gi